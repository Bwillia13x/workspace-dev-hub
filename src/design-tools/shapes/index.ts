/**
 * Shape Library Module - Fashion-specific shapes and trim library
 *
 * Provides:
 * - Button shapes (2-hole, 4-hole, shank, snap)
 * - Zipper components (teeth, slider, pull tab)
 * - Fasteners (hooks, eyes, buckles)
 * - Trim elements (ribbon, lace, piping)
 * - Garment construction shapes
 * - Technical flat sketch symbols
 */

export {
    ShapeLibraryManager,
    createShapeLibraryManager,
} from './shape-library';

export type {
    ShapeCategory,
    ShapeDefinition,
    ShapeOptions,
    ShapeInstance,
    ShapeLibraryEvents,
} from './shape-library';
